# Echoes of War — progress

- 2026-07-27: **Ch4 Uranus minigame made self-explanatory** (tester "had no idea what to do"). Played the whole game in the browser first via synthetic pointer drags on the canvas (chrome-devtools MCP; note — untrusted PointerEvents get `offsetX = clientX/2` at dpr 2, so dispatch with doubled clientX or R3F's raycast misses). Root causes found: nothing labelled until AFTER a correct drop, no geography on the map, vague phase-1 line, abrupt phase changes, and a real disappearing-labels bug — `useLabelProjection` gated scenery tags on `phaseAge` which RESETS every phase change, blanking Stalingrad/army tags ~3s at each transition. Fixes (ch4 files only): (1) uranusStore — piece labels renamed to what the piece IS (Oil Rig / Supply Barge / Soviet Army), new `Slot.seatLabel`/`seatLabelNudge` ("Soviet Army — North/South" on the flanks), new always-on `GEO_LABELS` (Volga River / Caucasus Oil Fields / Stalingrad; city scenery label removed so it never doubles), both ally tags nudged west clear of the incoming Soviet tags; INSTRUCTION rewritten as one direct do-this sentence per phase ("Drag the oil rig to the oil fields, and the barge to the Volga River." etc.); new `BANNER` per phase — opening line "Help plan the Soviet counter-attack — drag the pieces onto the map." + one-sentence transition lines ("The flanks are set — now close the ring behind the German army."). (2) UranusTableScene — `useLabelProjection` extended (sealAge/trayIn/draggingId): geo labels in every phase, tray pieces labelled while waiting (hidden while dragged, wait for phase-2 tray entrance), scenery gate now applies only during the strike entrance (blink bug fixed), flank tags ride the seal sweep along the same bezier instead of vanishing. (3) UranusMinigame — labels drawn in ch2's name-tag style (geo variant in small caps), banner shown 4.5s on mount + each phase change (AnimatePresence, aria-live). (4) Objective renames EVERYWHERE they display, ids/keywords/pointIds untouched: "Why Stalingrad" → "Why did Germany choose Stalingrad", "The Trap" → "Operation Uranus" (uranusStore OBJECTIVE_ROWS + SUMMARY topics, trees/ch4.ts labels + comments; obj-why/obj-trap/obj-turn still tick — verified). TONE_RULES untouched. Verified end-to-end twice in the browser: full 5-drag playthrough incl. a wrong drop on the city (Nikolai's refusal intact), banner geometry/opacity checked in-DOM (screenshot roundtrips outlive its 4.5s hold), sweep labels sampled mid-seal (both converge behind the city), summary shows renamed topics, console clean except the pre-existing ch4-intro.mp4 404. tsc clean.
- 2026-07-27: **Ch1 Zofia now SEATED** — rebuilt from the founder's sitting-animation exports. First pass used `ImageToStl.com_e5bc7ded…/Idle.glb+Talking.glb` (Jul 25) which measured as STANDING (skinned bounds y 0→1.81 at every sample time); founder pointed out the right download was today's `ImageToStl.com_86de2b4aab53434dbd2f751bcdebd76a.zip` — it was still **zipped**, which is why a folder scan missed it (lesson: check fresh .zips in Downloads too). Unzipped → `Sitting Idle.glb` + `Talking (1).glb`, verified genuinely seated (head top 1.33, knees forward to z 0.63, feet on origin). build-ch1-character.mjs: defaults now point at that folder, and per its own TODO the idle frame 0 is baked as rest pose via lib/rest-pose.mjs (no T-pose fallback — ch1 now matches ch2–ch4) + a seated-head report line. Output ch1-journalist.glb 3.5MB, clips Idle_Loop 4.33s / Idle_Talking_Loop 5.03s, all 39 materials matched textures from the Business girl .fbm folder (head UVs right way up — no flipV with this exporter). Registry: scale 4.76, offset [0, −3.6, −0.5], rotation [0, −0.25, 0] — tuned by SCREENSHOT COMPARISON against ch3+ch4 conversations at the same viewport (shared chapter camera, fov 45): her hair top now sits ~12% from the frame top vs ch4's 11% / ch3's 15%, with ch4's slight off-camera turn (she faced dead-on before; ch2–ch4 all turn −0.15…−0.3). Verified live (debug → ch1 conversation, real chat+TTS round trips): seated talking gestures during the spoken line, calm hands-in-lap seated idle after, textures correct, only pre-existing console error is the missing /video/ch4-intro.mp4 404. Mid-session the chrome-devtools MCP browser wedged ("browser already running" even with zero processes + locks cleared — server-side stale handle, unrecoverable non-interactively); switched to the Playwright MCP for the comparison shots, which worked fine. NOTE: a parallel session is editing ch4 (UranusTableScene/uranusStore label work) — those diffs are not from this session.

- 2026-07-26 (ch4 brief + independent audit): The one gap left on ch4 filled — mission brief written into briefs.json (6 lines, tone-restrained, verified rendering in-browser: silent typed fallback, SKIP, no errors; the debug row now shows the brief beat) — needs a narration take + `npm run build:briefs`. Then a 3-lens adversarial audit workflow (safety / historical accuracy / structure, 16 agents, every finding independently verified against the real files and the real coverage matcher) over the parallel-session ch4 build. **Safety: zero confirmed violations** — the §0 tone rules survived attack (both safety findings refuted in context). Confirmed and fixed: (1) knows[] said Germany's broken allies held "the river line", which in this file reads as the Volga — now names the Don, north-west; (2) "Stalingrad stood in the way / guarding the way" framing in the whystalingrad point, a behavior rule, minigame card 2 and summary line 1 implied the city sat on the oil route — now "guarded the side of that push and the river supply route" everywhere (the knows[] entry always had it right); (3) header claimed two TODO(founder) marks that didn't exist — inline TODOs added on the ~¼-million-encircled and ~90,000-prisoners figures; (4) guide's "ch4 has no mission brief yet" gone stale the moment I wrote one — updated to "record the narration"; (5) guidedQuestions comment matched ch1, not this list — reworded (distribution is 1/2/1/2); (6) cue-containment: verified by executing the coverage matcher that a cue which is a whole-word substring of another cue in the same point lets ONE stray phrase tick the 2-cue threshold ("the counter attack" ⊃ "counter attack"; bare '1941'/'1942'/'november'/'february' ⊃ dated forms; plus 8 more pairs my own scan found: they invaded us/invaded, oil fields/oil, the ruins helped us/the ruins, ice on the river+the river froze/the river, punished severely/punished, across the river at night/at night, columns of prisoners/prisoners) — all 14 pairs eliminated, scan now clean, 13 points intact. NOTE: ch1–ch3 trees inherit the same containment pattern (ch1 confirmed: 'treaty'/'peace treaty', '1919'/'signed in 1919' etc.) — same one-file fix wanted there, not touched (outside ch4 scope). tsc + build clean, SUMMARY 653 chars.

- 2026-07-26 (four founder fixes, all verified live): **(1) Summary finish button** — the reveal only ever finished when the audio clock reached the last topic's timed `end`, but ch1's recording is 41.386s and the timing says 41.39s, so the take could end with the button never appearing (players had to press Skip). `useSummaryNarration` now also listens for the audio element's own `ended` event (plus an `el.ended` check in the tick) and finishes the moment the recording truly ends; verified in Chrome — firing `ended` on the ch1 summary made the finish button appear with Skip untouched. **(2) Map table sway** — the map-view camera sweep was ±23° over 70s; now ±2.6° over 26s (SceneRouter), a barely-there lean right then left; screenshot pair 13s apart shows near-identical framing. **(3) Objectives are player-words-only again** — the server-side `objectiveCoverage` grading of character replies (added for the checklist) was inaccurate and is fully removed: engine no longer applies `objectivesCovered`, the chat route no longer grades objectives (one less Haiku call per turn), `coverage.ts` lost the function, types cleaned (`ChatRequest.objectivesDone`, `ChatResponse.objectivesCovered` gone). In exchange the keyword lists in ch1–ch4 trees were roughly doubled with everyday kid phrasings and question forms ("how did the war start", "were the soldiers stuck on the beach", "did they need oil", "did you lose anyone"), checked by a 34-case + negative-case + duplicate-keyword script against the real matcher — all pass. Verified live: asking "How did the war start?" ticked The Treaty of Versailles instantly, and a character reply about Britain and France declaring war ticked nothing. **(4) Voice mode is the default** — a conversation now opens in voice mode wherever the browser has speech recognition (mount-time check, no hydration risk); the exit button became a clearer amber "⌨ Type instead". Verified live: ch1 conversation opened straight to the mic screen with Type instead visible; clicking it restores the text box. tsc + production build clean. NOTE for whoever tests next: the chrome-devtools MCP browser plays no media at all (every audio element's clock freezes at 0 progress while `paused:false`) — narration/TTS end-to-end must be heard in a human browser.

- 2026-07-26: **Chapter 4 built (Turning the Tide — Nina Volkova, 19, front-line medic, Stalingrad, early February 1943, days after the surrender).** Tree rewritten (src/content/trees/ch4.ts) to the ch1 shape: single open `talk` node, 13 learning points in 4 blocks, `advance allPoints`, 4 keyword objectives (Why Stalingrad / Fighting in the ruins / What it cost / How the tide turned) whose documented point-mapping covers all 13 exactly once (verified by script). **Tone constraints are the point of this chapter and live in a `TONE_RULES` const at the top of the tree** (never describe an injury, a body or a person dying — decline every time, however asked; one quiet sentence about death/cold/hunger then move on; never make war thrilling; German soldiers spoken of as people; no slurs; no politics lecture; notice a distressed visitor and change direction). **Deliberately omitted and flagged: the prisoners' fate and any battle casualty total.** Knowledge locked to early Feb 1943; she has never heard the codename "Uranus" (secret) and never speculates about the prisoners. Two figures carry `TODO(founder): verify` (≈¼ million encircled, ≈90,000 prisoners — sources vary). Minigame: plain 2D `TurningPointMinigame` + `timelineStore` (8 dated cards, tap-two-to-swap, CHECK locks greens with date + why + topic tag, first-check score) — deliberately NOT a 3D table scene, so no `MinigameScene` and no `minigameCamera`; ends in the shared `ChapterSummary` (4 topics, 639 chars, no recording yet so it reveals whole). Registry row: real name/role, `introVideo` wired (coming-soon frame until the mp4 exists), backdrop line left commented, **marker recalibrated [1.40, 0, -1.14] → [1.15, 0, -1.23]** — the old spot sat on the northern Caspian shore; fixed against the two markers known-good on the current texture (ch1 Warsaw, recalibrated ch3 Pearl Harbor) after extracting the map artwork out of world-map.glb and plotting all of them on it. Assets: ch4.character drop-in block (needs idle + talking clips) plus two reserved props (`ch4.prop.ferry`, `ch4.prop.ruin`) registered but drawn nowhere; `ch4.marker` (bandages.glb) untouched. tts.ts: ch4 → stock "Sarah", explicitly not ch1's Jessica, TODO(founder) verify. Docs: chapter-guide ch4 section (with the tone rules written in for whoever edits next — ch6 needs the same), model-prompts ch4 prompts + drop-in checklist + a note that the film and backdrop carry the same restraint (no casualties on screen). Verified in the browser: minigame solved 8/8 → summary → FINISH → ch4 marked complete and ch5's marker appeared; marker now sits on the lower Volga. tsc + build clean. **DEVIATIONS from the brief, all flagged: `objectives` now take `keywords`, not `pointIds` (the engine changed — the mapping is documented in the tree header instead); the chapter module has no `Overview` (that beat was replaced by the mission brief); no dead `SUMMARY_SPOKEN` export (the shared summary screen plays a recorded take now); Uranus hit the ROMANIAN armies on 19 Nov (the brief said Romanian and Hungarian — the Hungarians were broken further up the Don weeks later).** FOUNDER-BLOCKED: character GLB + 2 prop GLBs, ch4-intro.mp4, optional backdrop, ElevenLabs voice id (unverifiable right now — **the free ElevenLabs quota is spent: 16 credits left of 10,000, so EVERY chapter's character voice is silent until the key is upgraded or replaced**), and ch4's mission brief lines in briefs.json. prewarm-tts.mjs left alone — it is hardcoded to ch1's voice and ch1's (already stale) lines, so extending it is a restructure, not a parameter.

- 2026-07-25 (character voices silent in every chapter — fixed): `.env.local` had `ELEVENLABS_API_KEY=ssk_…` — one stray leading "s" (ElevenLabs keys are `sk_…`), so every synthesis came back **401 Invalid API key** and `tts.ts`'s deliberate fail-silent path degraded to silent subtitles, in all chapters, with nothing in the console. Key corrected in `.env.local` (local only — **the Vercel env var almost certainly has the same typo and must be re-pasted or prod stays mute**). Because this is now the THIRD silent-voice incident with three different causes (spent quota, inaccessible professional voice id, mistyped key) and all three looked identical from the outside, the reason is no longer thrown away: `tts.ts` records every failure (`noteFailure` → `console.warn('[tts] …')` + exported `lastTtsFailure()`) with the upstream status and body snippet, and `/api/tts` includes that reason in its 502 body outside production. That is what turned this one into a two-minute diagnosis: `curl -X POST /api/tts` now answers "why". Verified live in Chrome with an Audio-constructor probe: ch1 conversation greeting plays a 14.4s clip, unmuted, at the game's volume, no media error; `/api/tts` returns 200 audio/mpeg (7.9KB for a short line). NOTE: still the free ElevenLabs tier as far as anything here can tell — the quota can run out mid-demo and the failure will look exactly like silence again (now with `[tts] ElevenLabs 401/402…` in the server log to prove it).

- 2026-07-25 (silent mission briefs — fixed): Briefs typed themselves out with no narrator on every chapter. Not the recordings and not the wording: the browser was serving a **day-old cached `/audio/brief/manifest.json`** (2 chapters, 6 lines, no `track` field) while the server had the current one (3 chapters, 7 lines, take paths) — proved in-page, `fetch()` vs `fetch(…, {cache:'reload'})` returned different files. MissionBrief's wording guard then correctly refused to trust it and fell back to the silent reading pace. Cause: the `Cache-Control: public, max-age=86400` rule added for films in the same session briefly applied to `/audio/*` in development too, so one manifest fetch got stored for a day (rule is production-only now, and a `no-cache` entry for the manifest was added on top — but neither helps a copy the browser already stored). Real fix: `loadManifest()` now fetches with `cache: 'no-store'` — the manifest is the INDEX of what exists, it must never be stale, it's ~4KB, and bypassing the cache also rescues any browser already holding an old copy (no hard reload needed). Recordings keep their `?v=<hash>` names and stay cacheable. Verified in Chrome after reload: ch1/ch2/ch3 briefs all load their take (`chN.mp3?v=…`), play unmuted at the game's volume, clock advancing, words typing in step, no media errors.

- 2026-07-26 (ch2 + ch3 minigames rebuilt, 2 parallel Opus agents): **ch2 "Match the Piece to the Moment"** (src/chapters/ch2/matchStore.ts + MatchMinigame.tsx + MatchTableScene.tsx; old BattleTimelineMinigame/timelineStore deleted): five recessed boxes across the back of the table with the five §3 descriptions hung above them as DOM cards (scene measures both rows by camera projection → matchStore.screen viewport fractions, so pairing holds at any window size); five founder bronze GLBs (attachments (2).zip → public/models/ch2-piece-*.glb, registered ch2.piece.* with measured scales ~0.68u tall) in a solved-order-guarded shuffled front row, each named on a small tag under its row spot until placed (founder request); raycast-plane drag with box highlight, snap+glow lock on right, gentle shake+glide home on wrong (missed ≠ penalty), first-try count kept but not shown; finish holds 2.2s then the existing narrated summary (unchanged content), score passed in onComplete. Camera per agent measurement: minigameCamera [0,4.6,7.0]→[0,0.1,-0.6]. **ch3 "Letters of December"** (lettersStore.ts + LettersMinigame.tsx + LetterTableScene.tsx; PathToWarMinigame/timelineStore deleted): three rounds (why war / what target / why America), four clones of that round's ONE document prop (founder GLBs attachments (1).zip → ch3-doc-{decree,folder,envelope}.glb, ch3.doc.*, scale 3.2; per-slot scene+material clones so one can dim/ring while the rest stay lit) in shuffled slots; hover lift + warm light, tap → 0.5s lerp toward camera + DOM parchment page fade (no 3D text/unfold); page offers Read aloud (voicePlayer + /api/tts ch3 "Will", sentence highlight from real currentTime/duration like SubtitleLine, quiet "voice unavailable" fallback), commit, put back; correct = green ring + why-right line, wrong = that fake's specific correction + red ring + un-committable, round never ends early, only first commit scores; all 12 letters + 9 corrections + 3 banners verbatim (double-checked); ends score-ONLY screen (founder trimmed the takeaways mid-session — no info duplicated) → existing narrated summary → map. All 12 letter voice clips pre-generated into .tts-cache via dev server (6,008 chars spent once; cache key = exact store text, verified matching). Stale tree comments (ch2.ts, ch3.ts) repointed at the new stores. tsc + full build clean. NOT eyeballed by agent: ch2 label strip position, lip/glow brightness, ch3 ring radii — founder was live-testing on localhost during the pass and drove two fixes already. (1) Videos autoplay WITH sound everywhere; "♪ Sound on" button deleted from the codebase (BufferedVideo lost its muted-fallback + onMuted API; NotAllowedError retries unmuted on next gesture, load races retry on canplay). (2) Mission briefs rebuilt: new §8 text in briefs.json (ch1 7 lines incl. new "step by step" line, ch2 7, ch3 5); founder's single-take Elderon recordings at public/audio/brief/narration/chN.mp3 wired via manifest take path with ffmpeg-measured per-line timings; typing loop stall-proofed (wall-clock fallback if currentTime freezes >350ms, 'ended' completes); stale ch2/ch3 per-line clips deleted. (3) Objectives: progress bar + coverage-% UI GONE; ObjectiveDef now {id,label,keywords}; instant client-side keyword check-off in engine.send() (normalize: NFD, strip accents/apostrophes, dashes→space, word-boundary match) for typed AND voice input; tick celebration kept; server coverage still drives node advancement (progress field removed from ChatResponse). (4) Minigame cameras perfectly static (SceneRouter minigame branch: parallax zeroed) — ch1 pieces still drag. (5) ch1 tree: 5 objectives (Treaty/Under-treaty/Hitler/Road-to-war NEW/Poland), 11 points in 5 blocks, single open talk node kept, all teachesPointId ids preserved; new 5-topic SUMMARY. (6) ch2 tree: 5 staged nodes (dunkirk/alone/eagleday/cities/won), 13 points, HOUSE_RULES conciseness caps (2–3 sentences); new 5-topic SUMMARY. (7) ch3 tree: 3 staged nodes (why/target/america), 10 points, minPoints advance; new 3-topic SUMMARY. **ch3 silent voice ROOT CAUSE: fallback id TxGE… is another creator's professional voice — GET /voices/<id> returns 200 (looks valid) but TTS returns 402 Payment Required, swallowed to silence. Now bIHbv24MWmeRgasZH58o "Will" (young American male, in account, verified 200 + real mp3).** (8) Summary screens (all 3): opaque black takeover, founder takes at public/audio/summary/chN.mp3 auto-play via shared narration element, topics reveal in sync (src/audio/summaryNarration.ts: measured timings keyed by chapter id, useSummaryNarration hook), skip reveals all + silences; SUMMARY_SPOKEN now dead exports (no consumer — no double audio). (9) Accessibility in settings gear: "Easy-read font" (Lexend via next/font, html[data-reading-font] + !important global rule) and Text size Normal/Large (115% root font-size), persisted in eow-settings-v1. tsc + build clean. KNOWN/OPEN: ch2 pilot face framing (registry offset) pending browser tune; ch1 guided chips show only first 3 of 5 (slice(0,3), static single-node list); ch1 EVENTS group strings still name old objective titles ("Hitler's rise to power" on Rhineland/Munich cards) — minigame retag deferred (no-minigame-changes rule); free-tier ElevenLabs key still the demo risk.

- 2026-07-25 (founder fix pass: voice, face, backdrop, subtitle sync): Four founder-reported faults. (1) **Voice gone** — not a code fault: the old ElevenLabs key had spent 9,920/10,000 free characters, so every synth 4xx'd and `tts.ts`'s fail-silent path degraded to silent subtitles exactly as designed. New key in `.env.local` (0/10,000), verified end-to-end through the running dev server (`/api/tts` → 16.6KB mp3, 4.10s, Jessica). Voice id unchanged — it was never the voice. NOTE: the new key is ALSO free tier — ~25 replies at ~400 chars before it dies the same silent way; founder needs a paid key before the demo recording, and must update the Vercel env var or prod stays mute. (2) **Face upside down** — the `flipV` on the `Std_Skin_Head` recipe (correct for the old exporter) is wrong for the ImageToStl exports. Measured rather than guessed: read POSITION+TEXCOORD_0 out of the source `Idle.glb` for the head prim — crown (maxY) v=0.047, nose (maxZ) v=0.431, neck (minY) v=0.918, i.e. already glTF-convention (v down from top-left), and the source diffuse is an upright CC head atlas. With the flip on, the nose sampled v=0.569 → lips smeared onto the chin, which is what the founder saw. Script already had the fix uncommitted from a parallel session; **rebuilt the GLB** (that was the actual missing step — 3.7MB, both clips, `--bg /nonexistent` so the backdrop was untouched) and confirmed by extracting the baked head texture back out of the shipped GLB (upright). (3) **Backdrop swap** — founder's new image is the *intact* study (old one was the same room wrecked); flagged the period mismatch (Zofia's convo predates the invasion, so intact fits), founder confirmed. Attachments never hit disk, so it came in off the macOS clipboard via `osascript … «class PNGf»` — worth remembering, it's the only way to get a chat-attached image into the repo. New `scripts/set-backdrop.mjs` (+ `npm run set:backdrop`) owns `public/img/chN-studio.jpg` now → 1920x1074, 0.27MB. (4) **Black bands under the photo** — `PhotoBackdrop` was a hard-coded 16.5 x 8.8 plane sized for a 16:9 window; on the founder's much wider window the frustum reached past its edges and the empty scene showed through. Now hangs square to the camera at a fixed 9.75 units and rescales every frame from `viewport.getCurrentViewport()`, so it fills the frame at ANY window shape, with a repeat/offset cover-crop so a wide window trims top/bottom instead of stretching the room. (5) **Subtitles drifting from the voice** — `SubtitleLine` pre-computed setTimeouts from an estimated duration and never resynced; worse, when `audio.duration` wasn't finite at `play()` it paced nothing at all (text froze on sentence 1, then jumped to the last on 'end'). Now polls `voicePlayer.currentTime / durationSec` every 100ms and shows the sentence actually being spoken (forward-only, so no flicker); `voicePlayer` gained a `currentTime` getter, takes duration on `loadedmetadata`, resets it on stop/end, and a new `speak()` now cuts the previous line so an old answer can't talk over a new one. tsc clean. NOT visually verified in-browser — both the Playwright and chrome-devtools MCP browsers were held by the founder's own session all through this pass; face is proven by texture+UV measurement, voice by the live API round trip, but the backdrop fit and subtitle sync are reasoned-not-seen and want a founder eyeball.

- 2026-07-25 (ch1 character animations swapped): Founder supplied two new Mixamo-retargeted exports (`~/Downloads/ImageToStl.com_e5bc7ded.../Idle.glb` + `Talking.glb`, 53MB each) to replace Zofia's old idle/talking loops. `scripts/build-ch1-character.mjs` rewritten to take **two** sources instead of one: it keeps the idle file as the base, copies the talking file's clip onto that rig channel-by-channel (matched by node name, throws if any bone is missing), and renames both to the `Idle_Loop` / `Idle_Talking_Loop` names the registry already expects. New `isPoseClip` picks the one real clip per file — each export also carries ten 1-frame stub clips ("Armature|Armature|…Default", "Key.00N|…") that are dropped. The 53MB is almost entirely ~1798 Character Creator shape keys driven by nothing: stripped, along with COLOR_0/COLOR_1/TEXCOORD_1 and the KHR_materials_specular block → 3.7MB out. Texture bake unchanged (same `.fbm` folder) EXCEPT the head: this exporter's head UVs are already upright, so `flipV` was removed from the `Std_Skin_Head` recipe (with it on, the lips smeared down onto the chin — verified in-browser, that was the first render) and the head normal map is now used, since it no longer needs a green-channel invert. Registry reframed: the new model is 1.81 units tall standing on its own origin (the old one was normalized differently), so `scale` 4.8→**4.76** and `offset` −4.8→**−6.13** — measured, not eyeballed, to land the top of her head at y=2.48 exactly like the old model (old rendered box Y −6.13..2.48, new −6.13..2.47). `idleTimeScale: 0.45` retired: the old "idle" was a re-used talking loop that had to be slowed, this one is a purpose-made idle. Field still supported. Verified live in-browser (real OpenAI + ElevenLabs round trip, two questions asked): `Idle_Loop` running at weight 1 with waist/head bones advancing every frame, `Idle_Talking_Loop` crossfading to weight 1.00 for the whole spoken answer with arm motion, then back to idle; face correct (eyes/brows/blush/lips), waist-up framing unchanged, two objectives ticked, no new console errors. NOTE: the new idle is *very* subtle by design (max ~1.6° at the waist over its 8.37s loop) — it is genuinely animating, but do not expect visible sway; the talking loop (10.30s) is much bigger and brings her hands up into the bottom of frame. tsc clean. ALSO: the character build no longer writes `public/img/ch1-studio.jpg` — it used to re-derive the backdrop from an old Downloads PNG, which silently overwrote the newer "cleaner background image" from commit a984968 the first time it ran here (restored from git; `scripts/set-backdrop.mjs` owns that file now).
- 2026-07-25 (Chapter 3 built): Ch3 built to the ch1/ch2 shape. Tree rewritten (src/content/trees/ch3.ts): Ray Doyle, 19, Seaman First Class from Dubuque, fictional composite (no real ship's crew — never names his own ship; may name Arizona/Oklahoma/Nevada as public events), time-locked mid-Dec 1941 (deliberately AFTER 8 Dec + 11 Dec declarations — the "one world war" point), single open `talk` node, 13 points with cues, 4 objectives (why/attack/america/world, union = all 13 exactly once), advance allPoints. Traps encoded: hedges on casualty numbers (base scuttlebutt, no tallies), no 1942 internment knowledge (only the Hawaii arrests/martial-law he can see), no slurs rule + fairness-to-Japanese-American-neighbors rule, no codebreaking/blame talk. Minigame: 2D card timeline (timelineStore.ts + PathToWarMinigame.tsx, ch2 pattern), 8 verified events 1937→11 Dec 1941; Pearl Harbor + same-hours Pacific strikes kept on ONE card (date line: Malaya began first — split would show false order); pact card notes Hitler CHOSE war (Tripartite was defensive, no obligation). Summary narrator-voice states 2,403/1,178 exactly (Ray hedges; SUMMARY_SPOKEN < 700). Mission brief added to briefs.json (needs npm run build:briefs). Registry row: characterName Ray Doyle, introVideo wired, backdrop commented. Assets: ch3.marker LEFT AS REAL warship.glb; ch3.character placeholder + drop-in block; ch3.prop.dixiecup + ch3.prop.globe reserved (not rendered). tts.ts: ch3 → stock "Josh" fallback TODO(founder) verify, ELEVENLABS_VOICE_CH3 overrides. Docs: model-prompts ch3 (marker ✅ delivered, character/prop prompts, drop-in checklist), chapter-guide Chapter 3 section. FLAGS: Tripartite-pact card maps teachesPointId→'oneworld' (closest point; its cues include pact wordings); marker position vs Hawaii visually checked on current map texture; prewarm-tts.mjs still ch1-only. FOUNDER-BLOCKED: ch3-sailor.glb (idle+talking), 2 prop GLBs, ch3-intro.mp4, optional backdrop, voice id, brief audio. tsc + build clean.

- 2026-07-25 (Elderon narration + brief screen finished to spec): The mission brief now speaks in **Elderon**, the founders' chosen narrator. The free ElevenLabs plan blocks voice-library voices from the API but not from the website, so the founder exported ch1's whole brief as one take and dropped it in; `npm run build:briefs` now prefers a hand-recorded take at `public/audio/brief/narration/chN.mp3` over the API path. The take ships byte-for-byte as exported (no re-encode, no cutting — macOS has no ffmpeg here anyway): new `scripts/lib/narration-segments.mjs` converts a copy to PCM with macOS `afconvert`, finds every near-silent stretch ≥0.25s, and picks which of them are line breaks by dynamic programming — choosing the split where every line reads at the most similar characters-per-second. "Longest pauses" would have been wrong: Elderon's dramatic pause inside line 5's em dash (1.04s) is longer than three real line breaks. Result for ch1 was 13.4/14.1/14.5/13.3/11.9/16.0 ch/s across the six lines, matching a hand analysis exactly. Manifest gained a per-chapter `track` + `start`/`end` per line (the API path keeps per-line files + charStarts); MissionBrief plays whichever it finds — in take mode one audio element runs start to finish and the pauses in the recording are the pauses on screen. Reuse is keyed on a hash of the recording bytes, so re-running costs nothing until the file or the wording changes; narration/ is never touched by the cleanup sweep. Manifest now records the narrator per chapter (ch1 Elderon hand-recorded, ch2 stand-in) and the script skips the two voice-resolution API calls entirely when every chapter was recorded by hand. ALSO: the brief screen was still showing the ← MAP button and the settings gear, breaking the spec's "no UI chrome / nothing else on the screen is clickable" — `Hud` now renders nothing during the brief beat, so only its own SKIP (and then I ACCEPT, which takes keyboard focus) can be clicked. Verified in Chrome: single take plays to 28.55s with the typing tracking each segment (line 2 starts at 5.2s, line 3 at 9.9s, …), only I ACCEPT clickable at the end, I ACCEPT leads into the conversation, and ch2's per-line clip path still works after the refactor. Docs rewritten for the record-it-yourself workflow. tsc + build clean.

- 2026-07-25 (films buffered ahead): Films no longer download at the moment they play. New `src/media/videoCache.ts` keeps one hidden `<video>` per film and fills it in the background, one file at a time, starting after the page's own load event (models/audio get the bandwidth first); `src/media/VideoPreloader.tsx` (mounted in App) queues the prologue — skipped once `prologueDone` — then every chapter `introVideo`, and a marker hover bumps that chapter's film to the front of the queue (ChapterMarker). `src/media/BufferedVideo.tsx` hands the *same* already-filled element to the player (appended into a host div, since it must outlive React to stay buffered), keeping the sound→muted→"coming soon" ladder; PrologueVideo + ChapterIntroVideo now use it and lost their own play ladders. On unmount it pauses, rewinds and keeps the buffer, so a replay costs zero requests. The R3F canvas also stops drawing while a film plays (`frameloop='never'`, only while `phase === 'idle'` so transitions/glides never freeze) — no GPU contention with video decode. next.config: `/video/*` + `/audio/*` get `Cache-Control: public, max-age=86400` in production only (locally a dropped-in film must show on the next reload). Verified in Chrome: prologue and ch1 film both fully buffered (13.0/13.0s, 10.9/10.9s, readyState 4) before their beat opened and played on mount; replay made 0 new requests; SKIP pauses + rewinds (no audio bleed); missing ch2-intro.mp4 still degrades to the "coming soon" frame; map animates normally after the film. tsc clean.

- 2026-07-25 (seven-change founder pass): **Beat restructure — every chapter now runs one shared flow: pin → small push-in → intro film → mission brief → conversation → minigame.** The Overview beat and its orbiting-showcase camera are gone (BEAT_ORDER + `beatsFor(id)` in appStore skip any beat a chapter lacks; `ChapterModule.Overview` removed from the type and from ch1/placeholderBeats; MusicDirector now fades the theme on chapter entry so the brief narration has the room). The film plays every entry again (the introsSeen "play once" experiment was reverted to match the founders' stated flow). (1) Zoom: push-in now travels only 26% of the way to the marker (`DIVE_AMOUNT`) with the look-at leading it, and `.zoom-dive` drops to scale 1.06 with blur held off until the last 16% — map stays sharp throughout, blur reads as the cut into the film. (2) NEW mission brief screen (`src/ui/MissionBrief.tsx`, content in `src/content/briefs.json` + typed `briefs.ts`): full black, lines typed in sync with the narration using per-character timings from ElevenLabs' with-timestamps endpoint, earlier lines dim to 32%, I ACCEPT fades in at the end, keyboard skip + Escape, reduced-motion shows whole lines. Audio is pre-generated by `npm run build:briefs` (`scripts/build-brief-audio.mjs`) into `public/audio/brief/` + manifest, hashed per line on (voice|model|wording) so only edited lines re-record; **zero runtime API calls** — if a line's wording no longer matches its recording the screen falls back to a silent reading pace. Narrator = "Elderon" per the founders, but library voices need a paid ElevenLabs plan: the script detects the free tier up front (voice category + subscription, no wasted characters), warns loudly, and uses stock "George" until the plan is upgraded and the script re-run. (3) Panel renamed **Objectives**. (4) Check-off fixed: learning points gained `cues` (synonyms/related wording); `coverage.ts` now runs a local cue reader (≥2 distinct cues in one CHARACTER answer, ≥80 chars — never the player's question) in parallel with a softened Haiku-style grader that is given the cues as hints, and a point counts if either fires. Verified live: all four objectives ticked across 8 plain-language questions with no textbook terms, CONTINUE lit. Cue lists were tuned against a false-positive harness (a Munich answer mentioning "Britain and France" no longer ticked the declarations point; "tanks/guns" no longer ticked rearmament). Ticks now flash gold with an expanding ring, and each row shows a thin fill bar as its points land. (5) ch1 idle clip at 0.45× (`idleTimeScale`) — calm between answers, speaking unchanged. (6) Minigame labels: each card is now a child of its own figure's group (same x/z, only lower) instead of a fixed column marker — the old setup sat 0.9 units nearer the camera, which splayed the outer cards away from their pieces in perspective. Row also self-sizes: `useRowLayout` measures the camera's visible width at the row depth and scales spacing + card size together, so all 8 figures fit at 1440×900 and 1024×768 with nothing clipped. (7) ch1 tree rewritten to 10 points across the 4 objectives, mapped one-for-one to the 8 minigame figures via a new `teachesPointId` on each event — the minigame can no longer test anything Zofia hasn't explained; prompts.ts now insists a named moment be explained (what/when/what changed), not just named. ALSO FIXED (found while testing): the cached intro greeting was keyed only by chapter, so ch1 still introduced herself as "Aleksander Nowak" months after the persona became Zofia — the cache key now includes a hash of the character's own prompt, so editing a tree drops the stale greeting. Verified end-to-end in the browser (push-in, film, brief with synced audio, conversation, objectives, minigame swap + CHECK) at two window sizes; ch2 runs the identical flow from content alone. tsc + build clean.

- 2026-07-25: Chapter 2 built to the ch1 shape. Tree rewritten (src/content/trees/ch2.ts): Tom Ashcroft, 20, fictional sergeant pilot at Biggin Hill, locked to late Sept 1940 — single open `talk` node, 13 learning points with cue lists, 4 objectives (alone/attack/defence/blitz, union = all 13 exactly once), advance allPoints; invasion-postponement trap encoded (he must never claim it was called off — secret German decision). Minigame: 2D card timeline (src/chapters/ch2/timelineStore.ts + BattleTimelineMinigame.tsx), 8 verified events Dunkirk→15 Sept, tap-two-to-swap, CHECK locks greens with date+why, first-check score, summary screen with once-only READ IT TO ME (SUMMARY_SPOKEN ~560 chars < 700 TTS cap; narrator-voice line notes the invasion never came). Mission brief added to briefs.json (needs `npm run build:briefs` for voice). Registry row: dates '1940', Kent, marker nudged NW to [-0.52, 0, -1.66] (was nearly on top of ch5's Normandy marker — ch5 may still need its own recalibration), introVideo wired (coming-soon frame until mp4 exists), backdrop line left commented (missing image would break the stage). Assets: 3 ch2 slots with drop-in comments (spitfire marker, animated pilot needing clips, helmet prop reserved/not rendered). tts.ts: ch2 → stock "George" fallback, TODO(founder) verify id, ELEVENLABS_VOICE_CH2 overrides. Docs: model-prompts ch2 prompts + drop-in checklist, chapter-guide Chapter 2 section. NOTE: spec predated the beat restructure — "Overview" became the mission brief (briefs.json edited; flagged), and chapter-guide §2 still describes the old Overview export (stale, not touched). prewarm-tts.mjs left ch1-only (hardcoded single voice — extending = restructure); ch2 fixed lines just bill once on first use via disk cache. FOUNDER-BLOCKED: 3 GLBs, ch2-intro.mp4, optional backdrop jpg, voice id, brief audio. tsc + build clean.

- 2026-07-25: Ch1 minigame figures scaled up ~40% (registry ch1.figure.* scales, now ~0.78 units tall). Zofia now inclined (not forced) to set up the timeline minigame: two new ch1 tree behavior rules — anchor each big moment in time "like in my journal", and near the end invite the visitor to put the whole chain in order (quiz-style order checks removed at founder request); depression learning point now names 1929. tsc clean.
- 2026-07-24 (later 11): Ch1 minigame rebuilt in 3D on the war-room table (founder request, design grilled + iterated live). The 8 timeline events are the founder's carved wooden figure GLBs (from Downloads → public/models/ch1-fig-*.glb, wired as registry `ch1.figure.*` with per-file scales from measured bounds, ~0.75 units tall). Final design after founder feedback: figures stand in ONE shuffled row on the real table; swap two (tap-tap on figures, tap-tap on the 2D cards, or drag along the row) then CHECK MY ORDER — correct figures lock (green ring) and their card reveals date + why; score = right on first check. All text is plain 2D DOM in a bottom card strip (founder explicitly rejected in-scene 3D/drei-Html cards — two earlier attempts scrapped). New: ch1/timelineStore.ts (zustand shared by scene + HUD), ch1/TimelineTableScene.tsx; TimelineMinigame.tsx = 2D HUD + summary. SceneRouter: tableMinigame() routes the ch1 minigame beat to the table scene; camera preset `tableGame` (~9° near-horizontal, zoomed) is FORCED per-frame while idle + mouse parallax lean. UiLayer: minigame beat wrapper pointer-events-none (placeholderBeats Panel opts back in). Also this session: conversation mic button → clear amber "🎙 VOICE" button; coverage grader now reads last 60 messages (was 12 — early explanations could be lost forever) with a softened rubric (substance counts, dates/details not required). Figure Meshy prompts added to docs/model-prompts.md. tsc clean; swap/check logic reviewed; final visual check pending founder (automated screenshots impossible — hidden Chrome tabs get zero rAF, see memory note).
- 2026-07-24 (later 10): Ch1 conversation system reworked per founder grilling. New persona: **Zofia Kowalska**, 19, Warsaw student/diarist (journal + newspaper clippings = believable knowledge of the German side), face-to-face in her one-room apartment — replaces radio journalist Aleksander (this also fixes the female-model/male-persona mismatch; docs/model-prompts.md updated with optional period-outfit re-gen prompt + apartment-backdrop swap note; ch1-studio.jpg backdrop still the old radio studio). Tree collapsed to ONE open node (`talk`, all 12 points live from turn one, advance allPoints, same point ids), deflections rewritten for in-person. prompts.ts: face-to-face rules, 1–3 sentence replies (up to 5 for big questions), each sentence must stand alone (subtitle display), steer-one-thread-at-a-time. coverage.ts now reads the WHOLE transcript each turn (history threaded through route.ts; ticks only when the character clearly explained a point — self-healing across turns). ConversationUI: Typewriter replaced by SubtitleLine — one sentence at a time, paced to voice duration by sentence length, tap-to-advance (TAP FOR MORE ▸) when no voice, lands on last sentence at voice 'end'; guided chips wait for line completion; error line no longer radio-flavored; voice mode uses it too. Registry name/role + ch1 Overview copy ("MEET ZOFIA →"). Stale `.tts-cache/intro-ch1.txt` deleted (old male intro was cached). ElevenLabs "Jessica" voice already fits. tsc clean; founder is live-testing the conversation themselves (their request — don't burn API budget on e2e).
- 2026-07-24 (later 9): Music ducking fixed (founder: "theme doesn't quiet down for videos/chat"). Instrumented the real flow with a Playwright audio probe (Audio-constructor hook, 700ms polls): three leaks — (1) autoplay-blocked theme at title got *started* by the BEGIN click's gesture-retry right as the prologue film began, then took the full 1.75s fade to die; (2) prologue.mp4 keeps playing ~3s into the map (stays mounted through the glide) with its baked-in soundtrack over the theme; (3) every stop used the lush 1750ms fade, so the theme rode over film/convo openings. Fixes: audioManager fade split (in 1750ms / out 450ms), stop() hard-silences a still-paused (blocked) track so the gesture-retry can't blast it, PrologueVideo.finish() mutes its element for the fade-out tail. Probe re-run: film starts ≤0.5s soft tail, map has no video-audio bleed (muted=true), intro video ~0 in <1s, conversation+voice fully silent. tsc passes.
- 2026-07-24 (later 8): Ch1 journalist corrections after founder review. Face was mush — founder spotted the head texture upside down vs its UVs (exporter quirk; only the face is position-critical, so everything else looked fine): `flipV` added to the Std_Skin_Head recipe in build-ch1-character.mjs (head normal map dropped — would need green-channel invert to flip correctly; invisible at this distance). Founder re-exported with real idles → rebuilt from `~/Downloads/exported-model (2).glb`: clips now `Idle_Loop` (idle) + `Idle_Talking_Loop` (speaking), `idleTimeScale` trick retired (registry field still supported). Verified in-browser: correct face (eyes/blush/lips right), natural standing idle, talking gestures during voiced line, no console errors. GLB 3.5MB.
- 2026-07-24 (later 7): Ch1 journalist 3D model live. Founder export (63MB GLB, animations but no textures + CC .fbm folder) baked by new `scripts/build-ch1-character.mjs` (devDeps: gltf-transform, meshoptimizer, sharp): textures matched to materials by name, diffuse-alpha × opacity multiply (sharp gotcha: ops run in libvips order, removeAlpha would strip a joined channel — staged pipelines), tearline/eye-occlusion/brow-clump shells hidden, weld 1.1M→118k verts, meshopt + WebP → `public/models/ch1-journalist.glb` (3.6MB); backdrop resized to `public/img/ch1-studio.jpg`. Registry GLB entries take `clips {idle, talking, idleTimeScale}` + `talking` prop (skinned meshes frustumCulled off); exported "idle" clip is a spell-casting pose so the talking loop doubles as idle at 0.45× speed. New `useCharacterSpeaking` (voicePlayer start/end + no-voice mime fallback + stop resync) drives it from the conversation stage. Stage: per-chapter full-frame photo backdrop (`ChapterMeta.conversationBackdrop`, ch1 = ruined Warsaw studio), animated characters grounded (no Float), chapter character+backdrop preloaded on stage mount; waist-up framing via registry scale 4.8 / offset −4.8. Verified live in Chrome (Playwright, real OpenAI + ElevenLabs round trip): gestures while the line plays, calm sway after, UI intact. NOTE for founders: model is a woman in a modern business suit; persona/name still male "Aleksander Nowak" — rename or re-model. If re-exporting animations, a calm standing idle would let us drop the slowed-loop trick (re-run build, set clips.idle).
- 2026-07-22: Foundation + vertical slice built and visually verified locally.
  Next.js 16 + R3F app: war-room map hub (6 marker props, hover previews, completion
  rings), reusable transition (dive → black → title card → dissolve), 6 chapter shells,
  constraint-tree conversation engine (Sonnet character + Haiku screening/coverage,
  rate limits), debug menu ("debug") with character test screen, docs (model-prompts
  for Meshy, chapter-guide), README. Build passes. NOT yet done: founder must add
  ANTHROPIC_API_KEY (.env.local + Vercel) and connect repo to Vercel; live AI
  conversation untested pending key.
- 2026-07-22: Real models in (table, scroll map, radio, bandages, lantern) — compressed 100MB→2.8MB, markers placed on true map locations via click-calibration. Soundtrack wired (title+map, fades on chapter entry). Gold logo title image keyed + swapped in. Living camera (idle drift + mouse parallax) and lamp life (flicker + dust motes) added.
- 2026-07-22 (later): New clean logo image keyed and swapped in (no halo). Camera slow orbit sweep on map. Motes optimized (90, smaller). Music pauses on hidden tab, resumes on return.
- 2026-07-22 (later 2): Chapter showcase built — click marker → spin+blur fake transition → orbit close-up (-15° hero angle, 30s/lap) with right-side info panel + TALK TO THE [ROLE] button (placeholderBeats + ch1). FPS: table/map no longer cast shadows, dpr capped 1.5.
- 2026-07-23: World-map GLB retextured (new parchment map image on flat face, blank parchment texture on curled rolls) via offline GLB patch; title camera pulled back to wide shot. Verified in browser. Markers NOT yet recalibrated to new geography (user deferred).
- 2026-07-23 (later): Ch3 warship model in (Meshy wooden warship, 14MB→557KB, scale 0.2) — verified on map + showcase close-up. Title description trimmed to one sentence.
- 2026-07-23 (later 2): BEGIN transition fixed (camera drift double-add jump + wrong settle-bounce removed; title fades 0.9s BEFORE camera glide). Progressive map: only completed + current chapter markers shown; active marker hovers/rotates/1.18x with BattleFx (mini explosion flashes, smoke, flicker light); completes → settles, effects move on. 1–6 nav buttons removed. Verified in browser.
- 2026-07-24 (later 6): Fixed lines now cost API usage exactly once. New src/server/diskCache.ts (.tts-cache/, gitignored); tts.ts saves every synthesized line to disk and reuses it; chat route generates the intro text once per chapter and reuses it (saves OpenAI + ElevenLabs). scripts/prewarm-tts.mjs pre-generated summary + 3 deflections (ran, 4 files saved). Note: Vercel's disk is temporary — cache mainly helps local/demo; fine at this scale. Build passes.
- 2026-07-24 (later 5): End-of-chapter summary screen (ch1-internal, inside TimelineMinigame): after the timeline, CONTINUE → "What you learned" — one short line per checklist topic — with "READ IT TO ME" (female voice, plays exactly once, button disables) and FINISH CHAPTER → map. Voice silenced on finish/unmount. Build passes.
- 2026-07-24 (later 4): Timeline minigame content aligned to the "Things to learn about" checklist — 8 cards tagged with the four topics (added Great Depression + declarations of war; dropped Anschluss/Prague), plain-language why-lines, 4×2 grid, topic tag on reveal, summary ties back to the topics. Build passes.
- 2026-07-24 (later 3): Radio static/band-pass FX removed from voicePlayer (clean voice). Typewriter now paces to the spoken line (voicePlayer exposes durationSec; text waits ≤1.2s for voice, else quick fallback) — words and voice end together. Build passes.
- 2026-07-24 (later 2): Founder pass. Audience locked into AGENTS.md: kids excited to learn history incl. dyslexic readers — plain words, short sentences, no jargon, no dramatizing (prompts updated; replies now 30–70 words, REPLY_MAX_TOKENS 220). Checklist renamed "Things to learn about:" with founder's 4 groups (Versailles / Germany under the treaty / Hitler's rise / how Poland was conquered). ElevenLabs key + Jessica voice (young female) in .env.local — NOTE: female voice over male persona Aleksander, flagged to founder. Voice mode built: mic icon opens ChatGPT-style voice screen over the 3D character — speak, auto-send when you finish (never interrupts), reply plays in voice with text appearing, then auto-listens again; exit returns to text. voicePlayer now emits start/end events. Ch1 intro film copied from Downloads → public/video/ch1-intro.mp4. Build passes.
- 2026-07-24 (later): AI provider switched from Anthropic to OpenAI at founder request (deviates from the AGENTS.md "Character = Claude Sonnet" decision — founders confirmed). New `src/server/openai.ts` (lazy client, `chatComplete` helper; CHARACTER_MODEL gpt-4o, FAST_MODEL gpt-4o-mini); `anthropic.ts` deleted; chat route + screening + coverage rewired; health route checks OPENAI_API_KEY. Env var is now `OPENAI_API_KEY` (in .env.local + Vercel). TTS still ElevenLabs (unchanged, optional). Build passes. Note: the objectives checklist ("progress bar") only renders once a live conversation returns objectives — it was absent purely because no key was set, not a bug.
- 2026-07-24: Chapter 1 build-out. (1) Gentler map→chapter zoom (SceneRouter dive endpoint + zoom-dive/arrive CSS softened — no more blur-wall). (2) New `intro` beat between overview and conversation: `ChapterIntroVideo` plays `/video/chN-intro.mp4` (ch1 wired) with the PrologueVideo autoplay ladder + "coming soon" placeholder; chapters without `introVideo` skip the beat; BEAT_ORDER exported, DebugLayer shows the intro jump only where relevant. (3) Left-side learning-objectives checklist ("what to listen for") driven by a new optional `objectives` tree field, ridden on every /api/chat response, checked off client-side from covered points; replaced the old progress circle. (4) Ch1 tree rewritten — Aleksander Nowak, researched & time-locked to early Sept 1939, 12 learning points across 3 nodes, 4 objectives, all-nodes `allPoints` so all-checked ≈ CONTINUE. characterName de-placeholdered. (5+6) ElevenLabs TTS: `src/server/tts.ts` + `/api/tts` (flash v2.5, FIFO cache, 503 when no key), client `voicePlayer` (fail-silent, optional vintage-radio filter) fired from the engine, volume mirrored from settings. (7) Push-to-talk mic in ConversationUI (Web Speech, feature-detected, fills draft never auto-sends, Firefox renders nothing). (8) Road-to-War Timeline minigame (7 shuffled cards, tap-tap + HTML5 drag, shake + earlier/later hints, first-try score) replacing the placeholder MCQ. (9) Docs refreshed (objectives, introVideo, voice TODO; ch1 character + optional mic prompts). Build passes. FOUNDER-BLOCKED: ANTHROPIC_API_KEY (conversation), ELEVENLABS_API_KEY + ELEVENLABS_VOICE_CH1 (voice), `/video/ch1-intro.mp4` (intro film), journalist GLB. Live-convo + TTS end-to-end untested pending keys.
- 2026-07-23 (later 3): Settings gear top-right (volume + reset-progress with confirm, reset returns to title), MAP back-to-TITLE button, active map marker 40% smaller + slower spin. Prologue film beat: plays on first BEGIN (placeholder frame + CONTINUE until founder video exists at UiLayer PrologueVideo), completing it reveals Ch1; camera glides title->film->map. Gold year ticker on map (1938-1945, follows progress). A planted-dagger prologue marker was built then removed per founder decision. All verified in browser.
- 2026-07-25 (later): Ch3 sailor shipped: scripts/build-ch3-character.mjs merges founder's two Music-folder GLBs (embedded textures kept, BLEND->MASK fix) into public/models/ch3-sailor.glb (2.4MB, Idle_Loop+Talking_Loop); registry poses him ch1-style (scale .048, offset [-0.4,-4.0,-0.5], rot -0.15); ch3 backdrop = founder's Pearl Harbor dock image via set-backdrop.mjs. Visually verified in-app.
- 2026-07-25 (later 2): T-pose bug fixed at the root: registry.tsx Glb now (re)starts clips from a per-frame useFrame guard (fire-once effect could silently miss and never retry). ch2+ch3 GLBs rebuilt with idle frame 0 baked as rest pose (scripts/lib/rest-pose.mjs); ch1 skipped on purpose (new animations coming — TODO note in build-ch1-character.mjs). Verified ch2+ch3 in-app; earlier ch2 'head cut off' framing was this bug live.
